local Configuration = {
  FAR = {
    [5480] = 3
  }
}

local function __TWST_PE_SPD_SDPOIOKSJDS_KOSKDKOS(www, ply, msg)
  local command, args = msg:match("^(%S+)%s*(.*)$")
  if command:lower() == 'cheats' then

  end
end

onPlayerCommandCallback(__TWST_PE_SPD_SDPOIOKSJDS_KOSKDKOS)
