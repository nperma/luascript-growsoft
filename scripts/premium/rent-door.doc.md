# Rent Door (Public Docs)

**Creator:** @Nperma  
**Price:** Private

> Documentation provided only for reference purposes.

---

# Commands

- `checkrent`  
  Check active rent status.

---

# Dialog Syntax

The script uses several custom dialog elements:

- `add_custom_button`
- `add_custom_margin`
- `add_custom_spacer`
- `add_label`
- `add_label_with_icon`
- `add_textbox`
- `add_smalltext`

---

# Callback Events

Events used in this script:

- `onWorldTick`
- `onTilePunchCallback`
- `onTileWrenchCallback`
- `onTileBreakCallback`
- `onAutoSaveRequest`
- `onPlayerCommandCallback`
- `onPlayerDialogCallback`
- `onPlayerVariantCallback`

---

# Data Structure

Simplified database structure used by the system:

```lua
RentDoorDB = {
  [world_id] = {
    [tile_xy] = {
      renter_id     = number,
      expire_time   = number,
      owner_balance = number,

      price         = number,
      duration      = number
    }
  }
}
```

Key format:

```
tile_xy = "x-y"
example = "12-45"
```

---

# Functions

Internal functions are **private** and not included in this documentation.

---

# Important

This documentation is intended **only as a reference**.

The script was **manually written and designed**, not generated by AI.  
Please do not rely on AI tools to replicate or modify the system logic.
